# Archived reports (pre fused virtual-axis)

Older comparisons, superseded by the live **fused virtual-axis** reports now in
[`../reports/`](../reports/). Kept for provenance; not part of the current Clifft-vs-fused-VA
result.

- `per_step_memory_3way/` — Clifft vs TTN vs near-Clifford **block** backend (resident bytes).
- `per_step_active_state/` — the 3-way active-state per-step CSVs / PNGs / tables (block + TTN).
- `per_step_flops/` — `FLOPS_TABLE` (Clifft / TTN / block) + the earlier fused **floor-only**
  FLOP count (`fused_va_flops.csv`, `fused_va_generate.py`), superseded by the full count.
- `measurement_dependency_trace/`, `targeted_peel/`, `per_step_flops_run.log` — earlier traces.
