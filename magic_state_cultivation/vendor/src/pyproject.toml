[project]
name = "magic-state-cultivation"
version = "0.0.0"
description = "Vendored circuit generation code from Strilanc/magic-state-cultivation"
requires-python = ">=3.12"
dependencies = [
    "pygltflib>=1.16.5",
]

[tool.setuptools.packages.find]
include = ["gen*", "cultiv*", "latte*"]
